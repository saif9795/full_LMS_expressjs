import mongoose from "mongoose";

const instructorModuleSchema = new mongoose.Schema({

});

const Instructormodule = mongoose.model("Instructormodule", instructorModuleSchema);

export default Instructormodule;